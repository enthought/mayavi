MayaVi2:
    - Core:
        - Better and correct persistence. (p)

    - Sources:
        - Multiblock data support.

    - Modules/Filters:
        
    - UI:
        - Easier access to the LUT editor.
        - Transfer editor for images. (g)
        - Enter_set/auto_set should be setup correctly for all entry
          dialogs so the user can control if things change on each
          keystroke.

    - Misc:
        - Support for animation.
        - Support for multiple renderers in a single scene.
        - Support for saving the default values of a TVTK object and
          persist them.

    - Documentation:

    - Mlab:
	- add texture support to Mlab 
